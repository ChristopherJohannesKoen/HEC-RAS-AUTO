# Fixtures

This folder stores small deterministic test fixtures used by `tests/`.

Current fixture files:
- `cross_sections_fixture.json`: canonical cross-section payload for parser/SDF tests.

Guidelines:
- Keep fixtures synthetic and minimal.
- Do not place runtime outputs or large raw datasets in this folder.
